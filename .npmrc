# Skip Chromium download — puppeteer is only used in local scraping scripts,
# not the deployed app. Vercel's build environment can't download Chromium.
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
puppeteer_skip_chromium_download=true